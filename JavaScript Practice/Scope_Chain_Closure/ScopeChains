// IModify your solution from lesson 1 so foo, in addition to lexically scoped variable bar, 
// contains a function zip
// which itself contains one variable lexically scoped called quux


function foo(){
    var bar;
    function zip(){
        var quux;
    }
}